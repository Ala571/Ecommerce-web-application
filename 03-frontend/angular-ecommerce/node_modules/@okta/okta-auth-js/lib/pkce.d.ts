import { TokenParams, CustomUrls, PKCEMeta, OAuthResponse } from './types';
declare function generateVerifier(prefix?: string): string;
declare function saveMeta(sdk: any, meta: PKCEMeta): void;
declare function loadMeta(sdk: any): PKCEMeta;
declare function clearMeta(sdk: any): void;
declare function computeChallenge(str: string): PromiseLike<any>;
declare function exchangeCodeForTokens(sdk: any, options: TokenParams, urls: CustomUrls): Promise<OAuthResponse>;
declare const _default: {
    DEFAULT_CODE_CHALLENGE_METHOD: string;
    generateVerifier: typeof generateVerifier;
    clearMeta: typeof clearMeta;
    saveMeta: typeof saveMeta;
    loadMeta: typeof loadMeta;
    computeChallenge: typeof computeChallenge;
    exchangeCodeForTokens: typeof exchangeCodeForTokens;
};
export default _default;
